{
  "name": "@moddef/transport-modbus-serial",
  "version": "0.1.0",
  "description": "ModDef Transport adapter for the modbus-serial package (Modbus RTU + TCP, Node only).",
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@moddef/core": "^0.1.0",
    "modbus-serial": "^8.0.0"
  }
}
